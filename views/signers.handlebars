<h2>These people already signed:</h2>

{{#each listOfSigners}}
    <p class="signers"><a href="{{url}}">{{first}} {{last}}</a> {{age}} <a href="/signers/{{city}}" class="city">{{city}}</a></p>
{{/each}}

<a href="/logout">log out</a>
